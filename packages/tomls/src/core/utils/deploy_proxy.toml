[clone.reyaCoreProxy]
source = "<%= settings.corePackage.concat('@proxy') %>"
target = "<%= settings.corePackage.concat('@proxy') %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
depends = ["var.cannonClonePackages"]
