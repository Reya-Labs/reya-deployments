include = [
    "./tokens/usdc.toml",
    "./tokens/usdt.toml",
    "./tokens/dai.toml",
    "./tokens/weth.toml",
    "./tokens/wbtc.toml",
]

[clone.reyaTokenRouter]
source = "<%= settings.tokensPackage.concat('@router') %>"
target = "<%= settings.tokensPackage.concat('@router') %>"
depends = ["var.cannonClonePackages"]
