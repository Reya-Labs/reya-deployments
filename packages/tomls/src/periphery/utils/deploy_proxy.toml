[clone.reyaPeripheryProxy]
source = "<%= settings.peripheryPackage.concat('@proxy') %>"
target = "<%= settings.peripheryPackage.concat('@proxy') %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
depends = ["var.cannonClonePackages"]
