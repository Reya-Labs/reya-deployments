[clone.reyaExchangePassProxy]
source = "<%= settings.exchangePassProxyPackage %>"
target = "<%= settings.exchangePassProxyPackage %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
depends = ["var.cannonClonePackages"]
