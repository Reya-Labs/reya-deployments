[clone.reyaRusdProxy]
source = "<%= settings.rUSDPackage.concat('@proxy') %>"
target = "<%= settings.rUSDPackage.concat('@proxy') %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
depends = ["var.cannonClonePackages"]
