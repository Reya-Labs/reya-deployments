[clone.reyaVltzProxy]
source = "<%= settings.vltzProxyPackage %>"
target = "<%= settings.vltzProxyPackage %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.envSalt %>"
depends = ["var.initial_proxy_vars", "var.cannonClonePackages"]
