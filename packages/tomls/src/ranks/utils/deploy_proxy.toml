[clone.reyaRanksProxy]
source = "<%= settings.ranksPackage.concat('@proxy') %>"
target = "<%= settings.ranksPackage.concat('@proxy') %>"
options.owner = "<%= settings.owner %>"
options.salt = "<%= settings.salt %>"
depends = ["var.cannonClonePackages"]
